# 品牌重塑提案总结

## 提案状态

**状态**: 📋 提案阶段（待审批）  
**创建日期**: 2026-01-17  
**预计工期**: 3-5 天  

## 当前进度

### ✅ 已完成的工作

1. **核心配置已更新**
   - ✅ `app/params/config.go`: BaseCoinUnit = "uaex", Bech32PrefixAccAddr = "aesc"
   - ✅ `x/evm/keeper/params.go`: BaseDenom = "uaex"
   - ✅ `cmd/seid/cmd/root.go`: MinGasPrices 使用 uaex
   - ✅ `poc-deploy/localnode/exmaple_genesis.json`: 已更新为 AESC 标识符
   - ✅ `depoly-scripts/localnode/aesc_genesis_template.json`: 已创建 AESC 模板

2. **提案文档已创建**
   - ✅ proposal.md - 提案概述
   - ✅ tasks.md - 详细任务清单（11 个阶段）
   - ✅ design.md - 技术设计文档
   - ✅ validation-checklist.md - 验证清单
   - ✅ README.md - 提案说明
   - ✅ 3 个规格增量文件（chain-identity, testing-infrastructure, deployment-tools）

3. **验证工具已创建**
   - ✅ check-sei-references.sh - 检查残留的 Sei 标识符
   - ✅ verify-aesc-identifiers.sh - 验证 AESC 配置

### ⏳ 待完成的工作

根据验证脚本的输出，以下工作仍需完成：

1. **Makefile 增强**（优先级：高）
   - ❌ 添加 `make test` 命令
   - ❌ 添加 `make test-unit` 命令
   - ❌ 添加 `make test-integration` 命令

2. **配置文件更新**（优先级：高）
   - ❌ `depoly-scripts/localnode/exmaple_genesis.json` - 仍包含 usei 和 sei1
   - ❌ `sei-cosmos/tests/fixtures/adr-024-coin-metadata_genesis.json` - 仍包含 usei

3. **代码文件替换**（优先级：中）
   - ❌ 约 177 个文件包含 `usei` 引用
   - ❌ 约 2 个文件包含 `sei1` 地址
   - 需要逐个审查和替换

4. **测试文件更新**（优先级：中）
   - ❌ 更新所有测试文件使用 AESC 标识符
   - ❌ 更新测试工具和辅助函数

5. **文档整理**（优先级：低）
   - ❌ 将 `tmp/AESC 链开发技术方案.md` 的内容整合到提案中（已部分完成）
   - ❌ 更新其他文档中的 Sei 引用

## 验证结果

### 当前验证状态

运行 `verify-aesc-identifiers.sh` 的结果：

```
✓ app/params/config.go: 4/4 检查通过
✓ EVM 模块配置正确
✗ Makefile: 0/3 检查通过（缺少测试命令）
✓ cmd/seid 配置正确
✗ 发现 4 个配置错误（主要是 genesis 文件）
```

### 需要修复的问题

1. **Makefile 测试命令缺失** - 需要添加
2. **部分 genesis 文件未更新** - 需要替换
3. **大量代码文件包含 usei** - 需要系统性替换

## 下一步行动

### 立即行动（如果提案批准）

1. **阶段 2: Makefile 增强**
   ```bash
   # 添加以下内容到 Makefile
   test: test-unit test-integration
   
   test-unit:
       @echo "Running unit tests..."
       go test -v -race -timeout=10m ./x/... ./app/... ./evmrpc/... ./precompiles/...
   
   test-integration:
       @echo "Running integration tests..."
       cd integration_test && go test -v -timeout=30m ./...
   ```

2. **阶段 3: 核心代码替换**
   - 使用自动化脚本批量替换 `usei` → `uaex`
   - 手动审查每个替换，确保不破坏功能
   - 每个模块替换后运行测试

3. **阶段 4: 测试文件更新**
   - 更新测试地址为 `aesc1` 格式
   - 更新测试金额使用 `uaex` 面额
   - 运行测试验证

### 风险提示

⚠️ **重要提示**：
- 这是一个大规模的替换工作，涉及约 177 个文件
- 必须分阶段进行，每个阶段都要运行测试验证
- 建议在独立的分支上进行，完成后再合并
- 保留详细的变更记录，便于回滚

## 资源和工具

### 验证脚本

```bash
# 检查残留的 Sei 标识符
./openspec/changes/rebrand-sei-to-aesc/scripts/check-sei-references.sh

# 验证 AESC 配置
./openspec/changes/rebrand-sei-to-aesc/scripts/verify-aesc-identifiers.sh
```

### 批量替换命令（谨慎使用）

```bash
# 查找所有包含 usei 的 Go 文件
find . -type f -name "*.go" ! -path "*/vendor/*" ! -path "*/go-ethereum/*" \
  -exec grep -l "usei" {} \; > files_to_update.txt

# 手动审查 files_to_update.txt 后，可以使用 sed 批量替换
# 注意：建议先在小范围测试
```

### 测试命令（实施后）

```bash
# 运行所有测试
make test

# 只运行单元测试
make test-unit

# 只运行集成测试
make test-integration
```

## 文档结构

```
openspec/changes/rebrand-sei-to-aesc/
├── README.md                    # 提案说明
├── SUMMARY.md                   # 本文档 - 提案总结
├── proposal.md                  # 提案概述
├── tasks.md                     # 详细任务清单
├── design.md                    # 技术设计文档
├── validation-checklist.md      # 验证清单
├── scripts/
│   ├── check-sei-references.sh      # 检查脚本
│   └── verify-aesc-identifiers.sh   # 验证脚本
└── specs/
    ├── chain-identity/
    │   └── spec.md              # 链标识规格增量
    ├── testing-infrastructure/
    │   └── spec.md              # 测试基础设施规格增量
    └── deployment-tools/
        └── spec.md              # 部署工具规格增量
```

## 批准检查清单

在批准此提案前，请确认：

- [ ] 已阅读并理解 proposal.md
- [ ] 已审查 design.md 中的技术决策
- [ ] 已查看 tasks.md 中的实施计划
- [ ] 已了解风险和缓解措施
- [ ] 已确认有足够的时间和资源（3-5 天）
- [ ] 已准备好测试环境
- [ ] 已了解回滚方案

## 联系和支持

如有问题或需要澄清，请：
1. 查看 design.md 中的"开放问题"部分
2. 运行验证脚本了解当前状态
3. 联系项目维护者讨论

---

**最后更新**: 2026-01-17  
**文档版本**: 1.0  
**提案 ID**: rebrand-sei-to-aesc

